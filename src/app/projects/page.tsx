import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Projects', description: 'Projects and engineering work by Anant Jain.' };
const projects = [{ name: 'Bulk Request Processing Platform', tech: 'Java · PL/SQL · Quartz', detail: 'High-volume enterprise request handling with query optimization and recovery workflows.' }, { name: 'Industry Registration Portal', tech: 'Spring Boot · Angular · REST', detail: 'Role-based registration workflow with slot booking and notifications.' }, { name: 'Technical Writing Hub', tech: 'Next.js · Supabase · SEO', detail: 'This site: a publishing platform for long-form engineering content.' }];
export default function ProjectsPage() { return <section className="section container"><div className="section-title"><span>Projects</span><h1>Things I have built, studied and shipped.</h1></div><div className="project-list">{projects.map((project) => <article className="project-row" key={project.name}><div><span>{project.tech}</span><h2>{project.name}</h2></div><p>{project.detail}</p></article>)}</div></section>; }
